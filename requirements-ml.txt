# Heavyweight ML dependencies for the real ALPR engine.
# Installed separately because they pull in ONNX Runtime and model weights.
# Without these, PlatePlayed automatically falls back to the stub detector,
# so the rest of the system (streams, DB, API, dashboard) still runs.
#
#   pip install -r requirements-ml.txt
#
# 0.4.x is the API verified against: OcrResult.confidence is a per-character
# list and DetectionResult.bounding_box exposes x1/y1/x2/y2 (see detector.py).
fast-alpr>=0.4.0
onnxruntime>=1.16.0
# Vehicle type detection (car/truck/bus/motorcycle) via a YOLO COCO model.
ultralytics>=8.0.0
